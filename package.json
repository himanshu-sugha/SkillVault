{
  "name": "skillvault",
  "version": "1.0.0",
  "description": "Privacy-preserving professional credential marketplace using Midnight Network",
  "author": "SkillVault Team",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "contract",
    "api",
    "skillvault-cli",
    "skillvault-ui"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace=contract && npm install --workspace=api && npm install --workspace=skillvault-cli && npm install --workspace=skillvault-ui",
    "compile:contract": "npm run compile --workspace=contract",
    "build:contract": "npm run build --workspace=contract",
    "build:api": "npm run build --workspace=api",
    "build:cli": "npm run build --workspace=skillvault-cli",
    "build:ui": "npm run build --workspace=skillvault-ui",
    "build:all": "npm run build:contract && npm run build:api && npm run build:cli && npm run build:ui",
    "dev:ui": "npm run dev --workspace=skillvault-ui",
    "test:contract": "npm run test --workspace=contract",
    "test:all": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
